__pycache__/
*.pyc
.env
*.egg-info/
.venv/
venv/

# PyInstaller outputs
build/
dist/

# Generated reports
*.xlsx

# Logs
shioaji.log
logs/

# Temp files
tmp/

# Scraped HTML samples
*.html

# Generated CSV outputs
*.csv

# pip install artifacts
=*

# Node
node_modules/

# Claude Code internal
.claude/

# Rule-file backups (see .claude/rules/04_maintenance.md)
*.bak-*

# Pickled StockData cache for signal_backtest --cache mode (multi-GB)
data/stock_cache/

# Binary ops tools (provenance documented in tools/README.md)
tools/*.exe

# TypeScript incremental build state
*.tsbuildinfo

# 大台期貨統一狀態 — Telegram-only, read locally by the bot. Kept out of git
# so the EOD whole-dir frontend push never sweeps it onto origin (an untracked
# copy that origin tracks would block the intraday worktree checkout).
frontend/public/data/futures_tx.json
